-- This file is generated by network:generate

local ffxiv_ipc_island_workshop_supply_demand = Proto("ffxiv_ipc_island_workshop_supply_demand", "FFXIV-IPC IslandWorkshopSupplyDemand")

local island_workshop_supply_demand_fields = {
  popularity           = ProtoField.uint8("ffxiv_ipc_island_workshop_supply_demand.popularity", "popularity", base.DEC),
  predicted_popularity = ProtoField.uint8("ffxiv_ipc_island_workshop_supply_demand.predicted_popularity", "predictedPopularity", base.DEC),
  supply_demand        = ProtoField.bytes("ffxiv_ipc_island_workshop_supply_demand.supply_demand", "supplyDemand", base.NONE),
}

ffxiv_ipc_island_workshop_supply_demand.fields = island_workshop_supply_demand_fields

function ffxiv_ipc_island_workshop_supply_demand.dissector(tvbuf, pktinfo, root)
  local tree = root:add(ffxiv_ipc_island_workshop_supply_demand, tvbuf)
  local len = tvbuf:len()

  -- dissect the popularity field
  local popularity_tvbr = tvbuf:range(0, 1)
  local popularity_val  = popularity_tvbr:le_uint()
  tree:add_le(island_workshop_supply_demand_fields.popularity, popularity_tvbr, popularity_val)

  -- dissect the predicted_popularity field
  local predicted_popularity_tvbr = tvbuf:range(1, 1)
  local predicted_popularity_val  = predicted_popularity_tvbr:le_uint()
  tree:add_le(island_workshop_supply_demand_fields.predicted_popularity, predicted_popularity_tvbr, predicted_popularity_val)

  -- dissect the supply_demand field
  local supply_demand_tvbr = tvbuf:range(2)
  local supply_demand_val  = supply_demand_tvbr:raw()
  tree:add(island_workshop_supply_demand_fields.supply_demand, supply_demand_tvbr, supply_demand_val)

  return len
end
