-- This file is generated by network:generate

local ffxiv_ipc_playtime = Proto("ffxiv_ipc_playtime", "FFXIV-IPC Playtime")

local playtime_fields = {
  playtime = ProtoField.uint32("ffxiv_ipc_playtime.playtime", "playtime", base.DEC),
}

ffxiv_ipc_playtime.fields = playtime_fields

function ffxiv_ipc_playtime.dissector(tvbuf, pktinfo, root)
  local tree = root:add(ffxiv_ipc_playtime, tvbuf)
  local len = tvbuf:len()

  -- dissect the playtime field
  local playtime_tvbr = tvbuf:range(0, 4)
  local playtime_val  = playtime_tvbr:le_uint()
  tree:add_le(playtime_fields.playtime, playtime_tvbr, playtime_val)

  return len
end
