-- This file is generated by network:generate

local db = require('ffxiv_db')

local ffxiv_ipc_market_board_purchase = Proto("ffxiv_ipc_market_board_purchase", "FFXIV-IPC MarketBoardPurchase")

local market_board_purchase_fields = {
  item_id  = ProtoField.uint32("ffxiv_ipc_market_board_purchase.item_id", "itemId", base.DEC, db.Item),
  quantity = ProtoField.uint32("ffxiv_ipc_market_board_purchase.quantity", "quantity", base.DEC),
}

ffxiv_ipc_market_board_purchase.fields = market_board_purchase_fields

function ffxiv_ipc_market_board_purchase.dissector(tvbuf, pktinfo, root)
  local tree = root:add(ffxiv_ipc_market_board_purchase, tvbuf)
  local len = tvbuf:len()

  -- dissect the item_id field
  local item_id_tvbr = tvbuf:range(0, 4)
  local item_id_val  = item_id_tvbr:le_uint()
  tree:add_le(market_board_purchase_fields.item_id, item_id_tvbr, item_id_val)

  -- dissect the quantity field
  local quantity_tvbr = tvbuf:range(8, 4)
  local quantity_val  = quantity_tvbr:le_uint()
  tree:add_le(market_board_purchase_fields.quantity, quantity_tvbr, quantity_val)

  return len
end
