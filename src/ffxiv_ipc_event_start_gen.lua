-- This file is generated by network:generate

local ffxiv_ipc_event_start = Proto("ffxiv_ipc_event_start", "FFXIV-IPC EventStart")

local event_start_fields = {
  actor_id = ProtoField.uint64("ffxiv_ipc_event_start.actor_id", "actorId", base.DEC),
  event_id = ProtoField.uint32("ffxiv_ipc_event_start.event_id", "eventId", base.DEC),
}

ffxiv_ipc_event_start.fields = event_start_fields

function ffxiv_ipc_event_start.dissector(tvbuf, pktinfo, root)
  local tree = root:add(ffxiv_ipc_event_start, tvbuf)
  local len = tvbuf:len()

  -- dissect the actor_id field
  local actor_id_tvbr = tvbuf:range(0, 8)
  local actor_id_val  = actor_id_tvbr:le_uint64()
  tree:add_le(event_start_fields.actor_id, actor_id_tvbr, actor_id_val)

  -- dissect the event_id field
  local event_id_tvbr = tvbuf:range(8, 4)
  local event_id_val  = event_id_tvbr:le_uint()
  tree:add_le(event_start_fields.event_id, event_id_tvbr, event_id_val)

  return len
end
