-- This file is generated by network:generate

local ffxiv_ipc_position = Proto("ffxiv_ipc_position", "FFXIV-IPC Position")

local position_fields = {
  x = ProtoField.float("ffxiv_ipc_position.x", "x", base.DEC),
  y = ProtoField.float("ffxiv_ipc_position.y", "y", base.DEC),
  z = ProtoField.float("ffxiv_ipc_position.z", "z", base.DEC),
}

ffxiv_ipc_position.fields = position_fields

function ffxiv_ipc_position.dissector(tvbuf, pktinfo, root)
  local tree = root:add(ffxiv_ipc_position, tvbuf)
  local len = tvbuf:len()

  -- dissect the x field
  local x_tvbr = tvbuf:range(0, 4)
  local x_val  = x_tvbr:le_float()
  tree:add_le(position_fields.x, x_tvbr, x_val)

  -- dissect the y field
  local y_tvbr = tvbuf:range(4, 4)
  local y_val  = y_tvbr:le_float()
  tree:add_le(position_fields.y, y_tvbr, y_val)

  -- dissect the z field
  local z_tvbr = tvbuf:range(8, 4)
  local z_val  = z_tvbr:le_float()
  tree:add_le(position_fields.z, z_tvbr, z_val)

  return len
end
