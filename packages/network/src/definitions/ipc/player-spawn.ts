import { Character } from './common/character'

export class PlayerSpawn extends Character {
  // @field(FieldType.uint, 2, 2)
  // u1b!: number
  // @field(FieldType.byte, 9)
  // u3c!: number
  // @field(FieldType.byte, 10)
  // u4!: number
}
